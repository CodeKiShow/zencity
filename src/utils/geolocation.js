export const calculateDistance = (lat1, lon1, lat2, lon2) => {
  // Distance calculation logic
  return 0;
};

export const isNearby = (userLocation, spotLocation, radius) => {
  // Proximity check logic
  return true;
};